import React from 'react';
import {Button} from "react-bootstrap";
import {ApplyBranding} from "../styles/apply-colors";

const handleOnClick = () => {
    return null
}

    const ApplyBrand = () => {
        return (
            <Button variant={"primary"} onClick={handleOnClick}>
                Change Color
            </Button>
        );
    };

    export default ApplyBrand;
